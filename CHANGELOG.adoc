// SPDX-FileCopyrightText: 2025 Shun Sakai
//
// SPDX-License-Identifier: Apache-2.0 OR MIT

= Changelog
:toc: preamble
:project-url: https://github.com/sorairolake/qrcode-rust2
:compare-url: {project-url}/compare
:issue-url: {project-url}/issues
:pull-request-url: {project-url}/pull

All notable changes to this project will be documented in this file.

The format is based on https://keepachangelog.com/[Keep a Changelog], and this
project adheres to https://semver.org/[Semantic Versioning].

== {compare-url}/v0.18.0\...HEAD[Unreleased]

=== Changed

* Compute globally optimal mode segmentation (ISO/IEC 18004 Annex J) instead of
  the previous greedy heuristic ({pull-request-url}/YYY[#YYY])

=== Fixed

* Fix suboptimal mode segmentation that could reject or needlessly upsize
  payloads which fit the symbol ({pull-request-url}/XXX[#XXX])

== {compare-url}/v0.17.2\...v0.18.0[0.18.0] - 2026-04-16

=== Added

* Add `eps::Color::new` ({pull-request-url}/120[#120])
* Add `svg::Color::new` ({pull-request-url}/120[#120])
* Re-export `csscolorparser` crate ({pull-request-url}/120[#120])

=== Changed

* Move `QrError` and `QrResult` to `error` module ({pull-request-url}/114[#114])
* Rename `QrError` to `Error` ({pull-request-url}/115[#115])
* Rename `QrResult` to `Result` ({pull-request-url}/115[#115])
* Change `eps::Color` field to private ({pull-request-url}/120[#120])
* Change `svg::Color` field to private ({pull-request-url}/120[#120])
* Use `f32` instead of `f64` for `eps::Color` ({pull-request-url}/121[#121])

=== Fixed

* Fix vertical positioning logic in `eps::Canvas` ({pull-request-url}/121[#121])

== {compare-url}/v0.17.1\...v0.17.2[0.17.2] - 2026-01-30

=== Changed

* Bump MSRV to 1.88.0 ({pull-request-url}/55[#55])

== {compare-url}/v0.17.0\...v0.17.1[0.17.1] - 2025-11-04

=== Added

* Re-export `types::QrError` ({pull-request-url}/48[#48])

== {compare-url}/v0.16.1\...v0.17.0[0.17.0] - 2025-11-01

=== Added

* Add `Renderer::quiet_zone_size` ({pull-request-url}/35[#35])

=== Changed

* Make `Bits::push_number_checked` public ({pull-request-url}/34[#34])
* Rename `Element::strlen` to `Element::str_len` ({pull-request-url}/38[#38])
* Rename `Renderer::quiet_zone` to `Renderer::has_quiet_zone`
  ({pull-request-url}/38[#38])
* Rename `Renderer::quiet_zone_size` to `Renderer::quiet_zone`
  ({pull-request-url}/38[#38])

=== Fixed

* Add missing MicroQR V3/L case to `is_half_codeword_at_end`
  ({pull-request-url}/36[#36])
* Use `saturating_sub` to prevent panic in padding calculation
  ({pull-request-url}/39[#39])

== {compare-url}/v0.16.0\...v0.16.1[0.16.1] - 2025-10-20

=== Added

* Implement `Default` for `EcLevel` ({pull-request-url}/30[#30])

=== Fixed

* Fix invalid QR code being generated ({pull-request-url}/27[#27])

== {compare-url}/v0.15.0\...v0.16.0[0.16.0] - 2025-10-19

=== Added

* Add benchmarks ({pull-request-url}/22[#22])

=== Changed

* Remove trailing LF from output PIC images ({pull-request-url}/21[#21])

== {compare-url}/v0.14.1\...v0.15.0[0.15.0] - 2025-10-18

=== Added

* Support `no_std` mode
* Add EPS rendering support
* Add rMQR code encoder
* Support automatic determines the smallest Micro QR code
* Re-export `image` crate ({pull-request-url}/13[#13])

=== Changed

* Improve rendered PIC quality
* Change XML declaration of SVG
* Reduce size of SVG output
* Change MSRV to 1.85.0
* Change behavior of `Version::is_micro` to return `false` if the parameter is
  out of the valid range
* Rename crate to `qrcode2`
* Make the `Error` trait available in `no_std` mode ({pull-request-url}/14[#14])

=== Removed

* Remove binary target
* Remove `bench` feature
* Remove deprecated methods ({pull-request-url}/3[#3])
